# 插件调用链流程图

这份文档说明这个 Zotero 插件模板从被 Zotero 加载，到把 UI 注入主窗口、偏好页和阅读器面板的大致调用链。

## 总览

模板的调用链可以概括成一句话：

Zotero 调用 bootstrap 生命周期，bootstrap 加载构建后的脚本，脚本创建全局插件单例，再通过 hooks 分发到各类注册函数，最后把菜单、列、面板和偏好页接到 Zotero UI 上。

## 启动主链路

```mermaid
flowchart TD
    A[Zotero 加载插件] --> B[addon/bootstrap.js startup]
    B --> C[注册 manifest.json 与 chrome 资源]
    C --> D[loadSubScript 加载构建产物 __addonRef__.js]
    D --> E[src/index.ts 创建 addon 单例]
    E --> F[把 addon 挂到 Zotero.addonInstance]
    F --> G[调用 addon.hooks.onStartup]
    G --> H[src/hooks.ts onStartup]
    H --> I[等待 Zotero 初始化完成]
    I --> J[initLocale]
    J --> K[registerPrefs]
    K --> L[registerNotifier]
    L --> M[registerShortcuts]
    M --> N[注册列与 ItemPane Section]
    N --> O[遍历现有主窗口并调用 onMainWindowLoad]
```

## 窗口 UI 注入链路

```mermaid
flowchart TD
    A[onMainWindowLoad win] --> B[createZToolkit]
    B --> C[注入 mainWindow.ftl]
    C --> D[显示启动进度窗]
    D --> E[registerStyleSheet]
    E --> F[registerRightClickMenuItem]
    F --> G[registerRightClickMenuPopup]
    G --> H[registerWindowMenuWithSeparator]
    H --> I[registerNormalCommandExample]
    I --> J[registerAnonymousCommandExample]
    J --> K[registerConditionalCommandExample]
    K --> L[触发 dialogExample]
```

## 偏好页链路

```mermaid
flowchart TD
    A[用户打开 Zotero 设置页] --> B[Zotero 显示 preferences.xhtml]
    B --> C[groupbox onload]
    C --> D[hooks.onPrefsEvent load]
    D --> E[registerPrefsScripts window]
    E --> F[初始化 addon.data.prefs]
    F --> G[updatePrefsUI]
    G --> H[创建 VirtualizedTable]
    H --> I[绑定选择、删除、搜索字符串逻辑]
    I --> J[bindPrefEvents]
    J --> K[复选框与输入框事件回流]
```

## 通知链路

```mermaid
flowchart TD
    A[Zotero 发生 tab/item/file 变化] --> B[Notifier callback]
    B --> C{addon 仍然存活吗}
    C -- 否 --> D[unregisterNotifier]
    C -- 是 --> E[hooks.onNotify]
    E --> F[按 event/type 过滤]
    F --> G[触发具体业务回调]
```

## 快捷键链路

```mermaid
flowchart TD
    A[用户按下快捷键] --> B[ztoolkit.Keyboard.register 回调]
    B --> C[hooks.onShortcuts]
    C --> D{动作类型}
    D -->|larger| E[exampleShortcutLargerCallback]
    D -->|smaller| F[exampleShortcutSmallerCallback]
```

## 命令面板链路

```mermaid
flowchart TD
    A[用户唤起 Prompt] --> B[Prompt 注册命令表]
    B --> C{是否满足 when 条件}
    C -- 否 --> D[命令不显示]
    C -- 是 --> E[显示命令]
    E --> F[用户选择命令或输入搜索]
    F --> G[执行 callback]
    G --> H[可能查询 Zotero 条目]
    H --> I[更新建议列表或跳转界面]
```

## 阅读器侧边栏链路

```mermaid
flowchart TD
    A[registerReaderItemPaneSection] --> B[Zotero 注册 reader section]
    B --> C[用户打开 reader 或切换条目]
    C --> D[onItemChange]
    D --> E[判断是否启用 section]
    E --> F[onRender 同步占位渲染]
    F --> G[onAsyncRender 异步加载数据]
    G --> H[更新标题、摘要、按钮状态]
    H --> I[用户点击 sectionButtons]
    I --> J[执行按钮回调]
```

## 卸载与清理链路

```mermaid
flowchart TD
    A[主窗口关闭] --> B[bootstrap onMainWindowUnload]
    B --> C[hooks.onMainWindowUnload]
    C --> D[ztoolkit.unregisterAll]
    D --> E[关闭可能存在的 dialog]

    F[插件禁用或热重载] --> G[bootstrap shutdown]
    G --> H[hooks.onShutdown]
    H --> I[清理注册项与窗口引用]
    I --> J[delete Zotero.addonInstance]
    J --> K[chromeHandle.destruct]
```

## 文件与职责对照

核心链路里最关键的文件分别是：

1. [addon/bootstrap.js](addon/bootstrap.js)：Zotero 生命周期入口。
2. [src/index.ts](src/index.ts)：创建全局插件单例与全局变量桥。
3. [src/addon.ts](src/addon.ts)：插件根对象和运行时状态。
4. [src/hooks.ts](src/hooks.ts)：统一初始化和事件分发中心。
5. [src/modules/examples.ts](src/modules/examples.ts)：把各类 Zotero 扩展点注册进 UI。
6. [src/modules/preferenceScript.ts](src/modules/preferenceScript.ts)：偏好页交互逻辑。
7. [addon/content/preferences.xhtml](addon/content/preferences.xhtml)：偏好页结构骨架。

## 真实开发时应如何理解这条链路

如果你后续要把这个模板做成正式插件，最重要的结构原则其实只有一条：

不要把业务逻辑直接塞进 bootstrap 或 hooks，而是把它们当成“接线层”。

更具体地说：

1. bootstrap 只负责把 Zotero 生命周期转发进你的脚本。
2. index 只负责创建全局单例。
3. hooks 只负责注册和路由。
4. 真正的 UI 渲染、数据请求、状态同步，应该拆到独立模块。

这样做的好处是：

1. 插件热重载时更容易定位问题。
2. UI 扩展点变多后不会失控。
3. 后续把模板示例替换成自己的业务模块时，改动范围更清晰。
